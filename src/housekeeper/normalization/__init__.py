"""Deterministic, versioned format-aware normalization profiles.

Each profile documents discarded information (``loss_characteristics``). Used for Tier-2/3
relationships; the raw cryptographic hash remains authoritative.
"""
