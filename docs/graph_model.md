# Graph model

Relationships use typed source/target ids, confidence, evidence JSON, and a relationship version. The projection builder supports named projection labels and hard caps of 5,000 nodes and 20,000 edges. The API is intended for aggregate universe, backup, project, duplicate, document-family, image-cluster, and selected-directory projections; evidence tables remain authoritative. Raw file nodes are disabled by default.
